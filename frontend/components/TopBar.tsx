"use client";

// Intentionally empty for now -- reserved for help/support entry points
// once those exist. The user menu lives in the sidebar instead.
export default function TopBar() {
  return <header className="h-16 shrink-0 border-b border-brand-border bg-background" />;
}
